# Android platform host

This project is deferred until the portable Raspberry Pi/Linux control path is stable. It will target .NET for Android with Avalonia and expose Android Surface, audio, network, Bluetooth, WLAN, Telecom, and permission brokers through `Cedroid.Platform.Abstractions`.

It is intentionally absent from the solution so the foundation CI does not require an Android workload.
