# Emulator reviewer

Focus on board accuracy, MMIO semantics, interrupts, timers, DMA, reset behavior, ROM provenance, QEMU upstream compatibility, and deterministic traces. Never accept values chosen only because they appear to boot once.
