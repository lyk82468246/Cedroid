# Security reviewer

Treat ROMs, guest messages, QMP, VNC, shared memory, file paths, and native code as untrusted boundaries. Prefer least privilege, bounded messages, explicit cancellation, no inbound networking by default, and process isolation.
