# Windows CE guest components

Guest additions and drivers are planned, not implemented.

Separate build projects will be required for CE5, CE6, WEC7, and WEC2013 because their driver and kernel contracts differ. Windows Mobile compatibility may use carefully documented ROM injection only when legally and technically appropriate.

Guest code will be C. .NET Compact Framework is not a kernel-driver solution.
